[project]
name = "mini-siem"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "watchdog",
    "sqlalchemy",
    "flask",
    "pyyaml",
    "python-dotenv"
]

[project.optional-dependencies]
dev = ["pytest", "ruff"]

[tool.pytest.ini_options]
pythonpath = ["src"]